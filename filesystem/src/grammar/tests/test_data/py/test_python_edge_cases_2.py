def format_strings():
    name = "World"
    age = 42

    # Simple f-string
    s1 = f"Hello, {name}!"

    # Nested braces in f-string
    s2 = f"Values: {{{name}, {age}}}"

    # Expression in f-string
    s3 = f"Age next year: {age + 1}"

    # Multi-line f-string
    s4 = f
