// Python-style
def python_function(arg1, arg2):
    return arg1 + arg2

// SQL-style
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(255)
);

// Objective-C style
@interface MyClass : NSObject
- (void)someMethod;
@end

// Rust/Scala style
trait Drawable {
    fn draw(&self);
}

// C/Rust struct
struct Point {
    x: i32,
    y: i32
}

// Enum
enum Color {
    RED,
    GREEN,
    BLUE
}