/// Adds two numbers.
///
/// # Arguments
/// * `a` - The first number.
/// * `b` - The second number.
///
/// # Returns
/// The sum of `a` and `b`.
#[inline]
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}